---
interface Props {
  title: string;
  className?: string;
}

const { title, className } = Astro.props;
---

<button type="button" class={`w-[122px] h-10 bg-primary text-white text-base rounded-md font-bold ${className}`}
  >{title}</button
>
